{
  "name": "repro",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "^14.15",
    "yarn": "^1.15.2"
  },
  "dependencies": {
    "@stoplight/prism-cli": "4.1.2"
  },
  "resolutions": {
    "@stoplight/prism-cli/json-schema-ref-parser": "9.0.8"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/node": "^14.17.5",
    "rimraf": "^3.0.2",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build": "rimraf ./dist && tsc --project ./tsconfig.build.json",
    "repro": "yarn node -r ts-node/register --unhandled-rejections=strict ./src/repro.ts"
  }
}
